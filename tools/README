Cedric Chauve, April 1st 2020

To install the headers and lib of Bio++ in this directory, on cedar
export PWD=`pwd`
install_bio++.sh $PWD
ln -s lib64 lib

To install DeCoSTAR, on cedar
module load boost/1.65.1 
install_decostar.sh $PWD

Zombi does not need installation as it is in python